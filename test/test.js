'use strict'

// TODO Add actual tests
var Force = require('../index.js');

// Set options
let options = {
  // autoPull: true,
  logging: {
    level: 'debug'
  }
}

// Create object
let force = new Force(options);

// force.init();
// force.pull();
// force.push();
// force.reset();
